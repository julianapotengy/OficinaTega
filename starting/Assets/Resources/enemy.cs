using UnityEngine;
using System.Collections;

public class enemy : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
		//Ray ray = ScreenPointToRay(transform.position);
	//Debug.DrawLine (ray.origin,ray.origin+ray.direction*100f, Color.black, 10);

		

	}
}
